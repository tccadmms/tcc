<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.admms.tcc.oasis">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@drawable/ic_logo"
        android:label="DBP - Serviços de Alimentação"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".controller.CadastroActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".controller.ItensDeAvaliacaoActivity" />
        <activity android:name=".controller.ArmazenamentoActivity" />
        <activity android:name=".controller.DocumentacaoActivity" />
        <activity android:name=".controller.EdificacaoActivity" />
        <activity android:name=".controller.DBPActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".controller.PopUpActivity"></activity>
    </application>

</manifest>